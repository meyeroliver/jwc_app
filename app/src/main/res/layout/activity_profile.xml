<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activities.ProfileActivity">

    <FrameLayout
        android:id="@+id/user_profile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <Button
        android:id="@+id/create_user"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/create_user"
        android:gravity="center"
        android:onClick="createNewUser"
        style="@style/Widget.MaterialComponents.ExtendedFloatingActionButton.Icon"

        app:icon="@drawable/ic_person_add_24dp"
        app:iconGravity="textStart"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <Button
        android:id="@+id/enable_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/enable_device_location"
        android:backgroundTint="@color/quantum_deeporangeA100"
        android:visibility="gone"
        android:gravity="center"
        android:onClick="enableLocation"
        style="@style/Widget.MaterialComponents.ExtendedFloatingActionButton.Icon"

        app:icon="@drawable/ic_room_24dp"
        app:iconGravity="textStart"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>